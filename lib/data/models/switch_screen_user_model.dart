class SwitchScreenUser {
  String userType;
  int id;
  String token;
  String userEmail;
  String userName;

  SwitchScreenUser(
      this.userType, this.id, this.token, this.userEmail, this.userName);
}
